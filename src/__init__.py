"""VoxelSense 3D — source modules.

Importable submodules:
    config            path management & tunables
    geometry          model loading + voxelization + surface sampling
    color_engine      CIELAB block palette + material weighting
    pipeline          OO façade (ModelLoader / VoxelEngine / ColorMatcher)
    viewer            PyVista + Matplotlib visualisation
    build_blocks_json offline palette builder from raw PNG textures
"""